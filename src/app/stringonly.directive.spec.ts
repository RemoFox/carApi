import { StringonlyDirective } from './stringonly.directive';

describe('StringonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new StringonlyDirective();
    expect(directive).toBeTruthy();
  });
});
